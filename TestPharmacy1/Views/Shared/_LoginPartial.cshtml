@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager


<ul class="navbar-nav regLog">
@if (SignInManager.IsSignedIn(User))
{
        <li class="nav-item">
            <a class="nav-link text-light btn-danger" asp-controller="Account" asp-action="Logout">Logout</a>
        </li>
}
else
{
    <li class="nav-item">
        <a class="nav-link text-light btn-primary" asp-controller="Account" asp-action="Register">Register</a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-light btn btn-danger" asp-controller="Account" asp-action="Login">Login</a>
    </li>
}
</ul>
<style>
    .regLog li a {
        font-size:1.2rem;
    }

    #userIcon:hover #dropdown {
        display:block;
    }
</style>